<Window x:Class="Pyrrha.Loader.AutoCad.UI.PyAppLoad"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        SizeToContent="WidthAndHeight"

        mc:Ignorable="d">
    <Grid x:Name="MainGrid" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width=".75*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <DockPanel Grid.Row="0" Grid.RowSpan="1" HorizontalAlignment="Stretch" Margin="0" Background="LightBlue" Name="DockPanel1">
                </DockPanel>
                <GridSplitter Grid.Column="1" 
                              BorderThickness="5" 
                              BorderBrush="DeepPink" 
                              ResizeDirection="Columns" 
                              Width="2" Height="Auto" 
                              HorizontalAlignment="Stretch" 
                              Margin="0" Name="GridSplitter1" />

                <TextBox Grid.Column="2" Width="Auto"></TextBox>

                <Button Grid.Column="3" Content="Search"></Button>
            </Grid>
            

            <ListView x:Name="DirectoryView"
                      MinWidth="570"
                      MinHeight="200"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch" >

            </ListView>
            <StackPanel>
                <Label Height="Auto" Padding="1" VerticalAlignment="Bottom" HorizontalAlignment="Left" Content="SomePath\to\somewhere"></Label>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>